# ── Core Framework ────────────────────────────────────
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic[email]==2.5.0
pydantic-settings==2.1.0

# ── Database ──────────────────────────────────────────
psycopg2-binary==2.9.10
# Database
sqlalchemy==2.0.23
alembic==1.12.1

# ── Security & Authentication ─────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.6
cryptography==41.0.7

# ── Environment & Config ──────────────────────────────
python-dotenv==1.0.0

# ── OCR & Image Processing ────────────────────────────
numpy==1.26.4

# ── Speech-to-Text (optional — uncomment if needed) ──
# SpeechRecognition>=3.10.0
# OCR & Image Processing
pytesseract==0.3.10
Pillow==10.1.0
opencv-python==4.6.0.66
# PaddleOCR - primary OCR engine (layout, tables, multilingual, Hindi)
paddlepaddle==2.6.2
paddleocr==2.7.3
# TrOCR - handwriting recognition fallback (prescriptions, FIRs)
transformers==4.38.2
torch==2.2.1
# PDF processing
PyMuPDF==1.23.8
pdfminer.six==20221105

# ── Utilities ─────────────────────────────────────────
python-dateutil==2.8.2
requests==2.31.0

# ── Testing (development) ─────────────────────────────
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2